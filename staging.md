## GIT STAGING

The Git staging area (or index) is an intermediate, virtual space acting as a "draft board" where you gather, select, and organize modified files before finalizing them into the repository with a commit. By separating git add (staging) from git commit (saving), you can curate precise, logical commits and avoid saving unwanted, messy